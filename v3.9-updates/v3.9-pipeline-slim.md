---
name: v3.9-pipeline-slim
description: v3.9 管线瘦身 — Phase 5 脚本化 + CI 模式可用化，3 项改动
type: project
---

Phase 5 当前是 goose session 含"尝试修复错误"指令，AI 在无任务上下文下猜测性改代码。CI 模式对所有失败统一 SKIP 且始终 exit 0。

**Why:** v3.8 修了失控问题但暴露深层设计缺陷 — Phase 5 应该是确定性验证不应该走 AI，CI 模式需要区分环境失败（应终止）和业务失败（应 skip-continue）。

**How to apply:** v3.9 改动 3 项 — D7(Phase5脚本化, P0)→D8(CI退出码, P1)→D9(CI摘要, P2)。全部方案在 `docs/plans/v3.9-pipeline-slim.md`。
